(function($) {
	$(document).ready( function() {
    	$('#list').qListSelect({
			// cursorPointer		: false,
			// disableSelection		: true,
			// initialFocus			: false,
			// keyboardActions		: false,
    	});
	});
	console.log('main.js loaded.');
}(jQuery));